part of '../landing_home.dart';

class Mobile extends GetWidget<LandingController> {
  const Mobile({super.key});

  @override
  Widget build(BuildContext context) {
     return const Center(
      child: Text('landing home mobile'),
    );
  }
}
